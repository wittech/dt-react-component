import { Component } from 'react';
import { API } from '@storybook/api';
interface State {
    selected: string;
    expanded: boolean;
}
export declare class BackgroundSelector extends Component<{
    api: API;
}, State> {
    state: State;
    change: ({ selected, name }: {
        selected: string;
        name: string;
    }) => void;
    onVisibilityChange: (s: boolean) => void;
    render(): JSX.Element;
}
export {};
