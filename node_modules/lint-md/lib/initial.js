"use strict";

var _astPlugin = require("ast-plugin");

_astPlugin.Ast.prototype.getSpec = function () {
  var _this$node$position = this.node.position,
      start = _this$node$position.start,
      end = _this$node$position.end;
  var lines = this.text.split('\n'); // 相同行

  if (start.line === end.line) {
    return lines[start.line - 1].substring(start.column - 1, end.column - 1);
  } // 不同行，截取多行


  var startLine = line[start.line - 1];
  var endLine = line[end.line - 1];
  return "".concat(startLine.substring(start.column - 1), "\n").concat(endLine.substring(0, end.column - 1));
};